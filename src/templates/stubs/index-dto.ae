<?php
namespace {{ DummyNamespace }};

class  {{ ModelName }}IndexDTO implements \JsonSerializable
{
{{ ConstructDataIndex }}

        //Relations
        {{ ConstructDataRelations }}

{{ SettersAndGettersIndex }}

        //Relations
        {{ SettersAndGettersRelations }}

    public function jsonSerialize():array
    {
        return [
            {{ SerializedDataIndex }}

            //Relations
            {{ SerializedDataRelations }}
        ];
    }
}
