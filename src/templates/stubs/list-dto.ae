<?php
namespace {{ DummyNamespace }};

class  {{ ModelName }}ListDTO implements \JsonSerializable
{
{{ ConstructData }}
//Relations
        {{ ConstructDataRelations }}

{{ SettersAndGetters }}
//Relations
        {{ SettersAndGettersRelations }}

    public function jsonSerialize():array
    {
        return [
            {{ SerializedData }}
            //Relations
                    {{ SerializedDataRelations }}
        ];
    }
}
